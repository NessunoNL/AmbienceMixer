# Backend Configuration
NODE_ENV=production
PORT=3000
AUDIO_DIR=/app/audio
DB_PATH=/app/data/ambience.db

# Note: SMB mount should be configured on the host system at /mnt/ambience
# This is mounted into the container via docker-compose.yml
